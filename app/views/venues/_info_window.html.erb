<div class="info-container d-flex justify-content-betwen">
  <div>
    <% if venue.photos.first == nil %>
      <%= image_tag('venue_placeholder_img.jpg', alt: "An image of a building outline", class: "venue-image") %>
    <% else %>
      <%= cl_image_tag venue.photos.first.key, alt: "An image of #{venue.name}", class: "venue-image" %>
    <% end %>
  </div>
  <div class="info-container-right">
    <h5><%= venue.name %></h5>
    <p><i class="fa-solid fa-location-dot"></i> <%= venue.address %></p>
    <%= link_to "More Details", venue_path(venue), class:"adept-btn medium info-cont-link" %>
  </div>
</div>
